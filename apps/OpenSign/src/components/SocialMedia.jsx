import React from "react";

// ALI Firmas: el pie del panel lateral no expone redes sociales de terceros.
// Se conserva el componente (y su punto de montaje en Sidebar.jsx) para no
// alterar la estructura del layout.
const SocialMedia = () => {
  return <React.Fragment />;
};

export default SocialMedia;
